package com.shakelang.util.colorlib.functional

import com.shakelang.util.colorlib.Formatting

/**
 * Make your foreground color black
 * @param contents the contents to make black
 */
fun fgBlack(contents: String) = "${Formatting.FGColor.BLACK}$contents${Formatting.RESET}"

/**
 * Make your foreground color red
 * @param contents the contents to make red
 */
fun fgRed(contents: String) = "${Formatting.FGColor.RED}$contents${Formatting.RESET}"

/**
 * Make your foreground color green
 * @param contents the contents to make green
 */
fun fgGreen(contents: String) = "${Formatting.FGColor.GREEN}$contents${Formatting.RESET}"

/**
 * Make your foreground color yellow
 * @param contents the contents to make yellow
 */
fun fgYellow(contents: String) = "${Formatting.FGColor.YELLOW}$contents${Formatting.RESET}"

/**
 * Make your foreground color blue
 * @param contents the contents to make blue
 */
fun fgBlue(contents: String) = "${Formatting.FGColor.BLUE}$contents${Formatting.RESET}"

/**
 * Make your foreground color purple
 * @param contents the contents to make purple
 */
fun fgPurple(contents: String) = "${Formatting.FGColor.PURPLE}$contents${Formatting.RESET}"

/**
 * Make your foreground color cyan
 * @param contents the contents to make cyan
 */
fun fgCyan(contents: String) = "${Formatting.FGColor.CYAN}$contents${Formatting.RESET}"

/**
 * Make your foreground color white
 * @param contents the contents to make white
 */
fun fgWhite(contents: String) = "${Formatting.FGColor.WHITE}$contents${Formatting.RESET}"

/**
 * Make your foreground color gray
 * @param contents the contents to make gray
 */
fun fgBrightBlack(contents: String) = "${Formatting.FGColor.BRIGHT_BLACK}$contents${Formatting.RESET}"

/**
 * Make your foreground color gray
 * @param contents the contents to make gray
 */
fun fgGrey(contents: String) = fgBrightBlack(contents)

/**
 * Make your foreground color bright red
 * @param contents the contents to make bright red
 */
fun fgBrightRed(contents: String) = "${Formatting.FGColor.BRIGHT_RED}$contents${Formatting.RESET}"

/**
 * Make your foreground color bright green
 * @param contents the contents to make bright green
 */
fun fgBrightGreen(contents: String) = "${Formatting.FGColor.BRIGHT_GREEN}$contents${Formatting.RESET}"

/**
 * Make your foreground color bright yellow
 * @param contents the contents to make bright yellow
 */
fun fgBrightYellow(contents: String) = "${Formatting.FGColor.BRIGHT_YELLOW}$contents${Formatting.RESET}"

/**
 * Make your foreground color bright blue
 * @param contents the contents to make bright blue
 */
fun fgBrightBlue(contents: String) = "${Formatting.FGColor.BRIGHT_BLUE}$contents${Formatting.RESET}"

/**
 * Make your foreground color bright purple
 * @param contents the contents to make bright purple
 */
fun fgBrightPurple(contents: String) = "${Formatting.FGColor.BRIGHT_PURPLE}$contents${Formatting.RESET}"

/**
 * Make your foreground color bright cyan
 * @param contents the contents to make bright cyan
 */
fun fgBrightCyan(contents: String) = "${Formatting.FGColor.BRIGHT_CYAN}$contents${Formatting.RESET}"

/**
 * Make your foreground color bright white
 * @param contents the contents to make bright white
 */
fun fgBrightWhite(contents: String) = "${Formatting.FGColor.BRIGHT_WHITE}$contents${Formatting.RESET}"

/**
 * Make your foreground color black
 * @param contents the contents to make black
 */
fun black(contents: String) = fgBlack(contents)

/**
 * Make your foreground color red
 * @param contents the contents to make red
 */
fun red(contents: String) = fgRed(contents)

/**
 * Make your foreground color green
 * @param contents the contents to make green
 */
fun green(contents: String) = fgGreen(contents)

/**
 * Make your foreground color yellow
 * @param contents the contents to make yellow
 */
fun yellow(contents: String) = fgYellow(contents)

/**
 * Make your foreground color blue
 * @param contents the contents to make blue
 */
fun blue(contents: String) = fgBlue(contents)

/**
 * Make your foreground color purple
 * @param contents the contents to make purple
 */
fun purple(contents: String) = fgPurple(contents)

/**
 * Make your foreground color cyan
 * @param contents the contents to make cyan
 */
fun cyan(contents: String) = fgCyan(contents)

/**
 * Make your foreground color white
 * @param contents the contents to make white
 */
fun white(contents: String) = fgWhite(contents)

/**
 * Make your foreground color gray
 * @param contents the contents to make gray
 */
fun grey(contents: String) = fgGrey(contents)

/**
 * Make your foreground color bright red
 * @param contents the contents to make bright red
 */
fun brightRed(contents: String) = fgBrightRed(contents)

/**
 * Make your foreground color bright green
 * @param contents the contents to make bright green
 */
fun brightGreen(contents: String) = fgBrightGreen(contents)

/**
 * Make your foreground color bright yellow
 * @param contents the contents to make bright yellow
 */
fun brightYellow(contents: String) = fgBrightYellow(contents)

/**
 * Make your foreground color bright blue
 * @param contents the contents to make bright blue
 */
fun brightBlue(contents: String) = fgBrightBlue(contents)

/**
 * Make your foreground color bright purple
 * @param contents the contents to make bright purple
 */
fun brightPurple(contents: String) = fgBrightPurple(contents)

/**
 * Make your foreground color bright cyan
 * @param contents the contents to make bright cyan
 */
fun brightCyan(contents: String) = fgBrightCyan(contents)

/**
 * Make your foreground color bright white
 * @param contents the contents to make bright white
 */
fun brightWhite(contents: String) = fgBrightWhite(contents)

/**
 * Make your background color black
 * @param contents the contents to make black
 */
fun bgBlack(contents: String) = "${Formatting.BGColor.BLACK}$contents${Formatting.RESET}"

/**
 * Make your background color red
 * @param contents the contents to make red
 */
fun bgRed(contents: String) = "${Formatting.BGColor.RED}$contents${Formatting.RESET}"

/**
 * Make your background color green
 * @param contents the contents to make green
 */
fun bgGreen(contents: String) = "${Formatting.BGColor.GREEN}$contents${Formatting.RESET}"

/**
 * Make your background color yellow
 * @param contents the contents to make yellow
 */

fun bgYellow(contents: String) = "${Formatting.BGColor.YELLOW}$contents${Formatting.RESET}"

/**
 * Make your background color blue
 * @param contents the contents to make blue
 */
fun bgBlue(contents: String) = "${Formatting.BGColor.BLUE}$contents${Formatting.RESET}"

/**
 * Make your background color purple
 * @param contents the contents to make purple
 */
fun bgPurple(contents: String) = "${Formatting.BGColor.PURPLE}$contents${Formatting.RESET}"

/**
 * Make your background color cyan
 * @param contents the contents to make cyan
 */
fun bgCyan(contents: String) = "${Formatting.BGColor.CYAN}$contents${Formatting.RESET}"

/**
 * Make your background color white
 * @param contents the contents to make white
 */
fun bgWhite(contents: String) = "${Formatting.BGColor.WHITE}$contents${Formatting.RESET}"

/**
 * Make your background color bright red
 * @param contents the contents to make bright red
 */
fun bgBrightRed(contents: String) = "${Formatting.BGColor.BRIGHT_RED}$contents${Formatting.RESET}"

/**
 * Make your background color bright green
 * @param contents the contents to make bright green
 */
fun bgBrightGreen(contents: String) = "${Formatting.BGColor.BRIGHT_GREEN}$contents${Formatting.RESET}"

/**
 * Make your background color bright yellow
 * @param contents the contents to make bright yellow
 */
fun bgBrightYellow(contents: String) = "${Formatting.BGColor.BRIGHT_YELLOW}$contents${Formatting.RESET}"

/**
 * Make your background color bright blue
 * @param contents the contents to make bright blue
 */
fun bgBrightBlue(contents: String) = "${Formatting.BGColor.BRIGHT_BLUE}$contents${Formatting.RESET}"

/**
 * Make your background color bright purple
 * @param contents the contents to make bright purple
 */
fun bgBrightPurple(contents: String) = "${Formatting.BGColor.BRIGHT_PURPLE}$contents${Formatting.RESET}"

/**
 * Make your background color bright cyan
 * @param contents the contents to make bright cyan
 */
fun bgBrightCyan(contents: String) = "${Formatting.BGColor.BRIGHT_CYAN}$contents${Formatting.RESET}"

/**
 * Make your background color bright white
 * @param contents the contents to make bright white
 */
fun bgBrightWhite(contents: String) = "${Formatting.BGColor.BRIGHT_WHITE}$contents${Formatting.RESET}"

/**
 * Make your text bold
 * @param contents the contents to make bold
 */
fun bold(contents: String) = "${Formatting.BOLD}$contents${Formatting.RESET}"

/**
 * Make your text italic
 * @param contents the contents to make italic
 */
fun italic(contents: String) = "${Formatting.ITALIC}$contents${Formatting.RESET}"

/**
 * Make your text underlined
 * @param contents the contents to make underlined
 */
fun underline(contents: String) = "${Formatting.UNDERLINE}$contents${Formatting.RESET}"

/**
 * Make your text reverse
 * @param contents the contents to make reverse
 */
fun reverse(contents: String) = contents.reversed()

/**
 * Invert colors
 * @param contents the contents to invert
 */
fun invert(contents: String) = "${Formatting.INVERT}$contents${Formatting.RESET}"

fun join(vararg strings: String) = strings.joinToString("")
